===========================================================================
  유효성 체크 TODO 목록
  작성일: 2026-04-14
  기준: winiTest2 프로젝트 현재 상태
===========================================================================

[범례]
  [F] = 프론트엔드(JSP/JS)에서만 처리 중
  [B] = 백엔드(Controller/Service)에서만 처리 중
  [FB] = 프론트 + 백엔드 양쪽 처리 중
  [TODO] = 아직 구현 안 됨 (처리 필요)
  [OK] = 현재 구현 완료


===========================================================================
1. 회원가입 (register.jsp / UserController / UserMapper.xml)
===========================================================================

[OK][F]  아이디 형식 실시간 체크 (5~15자, 영문+숫자만)
[OK][F]  비밀번호 형식 실시간 체크 (10~25자, 영문+숫자+특수문자 조합)
[OK][F]  비밀번호 확인 일치 체크
[OK][F]  이름 공백 제거 후 빈값 체크
[OK][F]  아이디 중복확인 미수행 시 제출 차단
[OK][FB] 아이디 중복 (Ajax: F에서 idCheck.do 호출 → B에서 COUNT 쿼리)

[TODO][F]  아이디 input maxlength 속성 없음 → maxlength="15" 추가 필요
[TODO][F]  비밀번호 input maxlength 속성 없음 → maxlength="25" 추가 필요
[TODO][F]  이름 input maxlength 속성 없음 → maxlength="50" 추가 필요
[TODO][B]  서버 측 아이디/비밀번호/이름 형식 검증 없음
           → JS 우회 시 형식 외 값이 DB에 그대로 저장됨
[TODO][B]  비밀번호 평문 저장 중 (DB에 user_pw 컬럼이 VARCHAR 평문)
           → BCrypt 등 단방향 해시 암호화 적용 필요 (보안 필수)

===========================================================================
2. 로그인 (login.jsp / UserController / UserMapper.xml)
===========================================================================

[OK][B]   아이디+비밀번호 DB 조회 일치 여부 확인
[OK][B]   로그인 실패 시 오류 메시지 표시 (error=1 파라미터)

[TODO][F]  아이디/비밀번호 빈값 체크 없음 (submit 시 서버로 그냥 전송됨)
           → validate() 함수 추가 또는 required 속성 추가 필요
[TODO][F]  아이디 maxlength 없음 → maxlength="15" 추가 필요
[TODO][F]  비밀번호 maxlength 없음 → maxlength="25" 추가 필요
[TODO][B]  로그인 시도 횟수 제한 없음 (무제한 시도 가능)
           → 일정 횟수 이상 실패 시 계정 잠금 또는 CAPTCHA 고려 (보안)
[TODO][B]  비밀번호가 평문으로 DB에서 조회됨 (암호화 적용 시 변경 필요)

===========================================================================
3. 게시글 작성 (write.jsp / BoardController.writeProc / BoardMapper)
===========================================================================

[OK][F]   제목 빈값 체크 (validate 함수)
[OK][F]   내용 빈값 체크 (validate 함수)
[OK][F]   비밀번호 빈값 체크 (validate 함수)
[OK][F]   파일 개수 5개 초과 체크 (change 이벤트)
[OK][F]   파일 개별 크기 10MB 초과 체크 (change 이벤트)
[OK][F]   파일 총 용량 50MB 초과 체크 (change 이벤트)

[TODO][B]  제목/내용/비밀번호 빈값 서버 검증 없음
           → JS 우회 시 빈 제목/내용이 DB에 저장됨
[TODO][B]  파일 개수/크기 서버 검증 없음
           → JS 우회(직접 HTTP 요청)로 제한 없이 업로드 가능
           → Spring multipart 설정에서 maxFileSize, maxRequestSize 제한 추가 필요
              (context-datasource.xml 또는 web.xml 의 multipart-config)
[TODO][B]  파일 확장자 검증 없음 (exe, jsp 등 위험 파일도 업로드 가능)
           → 허용 확장자 화이트리스트 또는 차단 블랙리스트 처리 필요
[TODO][F]  비밀번호 maxlength="50" 있으나 형식 제한 없음
           → 게시글 비밀번호 규칙을 사용자에게 안내할지 결정 필요

===========================================================================
4. 게시글 수정 (edit.jsp / BoardController.editProc / BoardMapper)
===========================================================================

[OK][F]   제목 빈값 체크 (validate 함수)
[OK][F]   내용 빈값 체크 (validate 함수)
[OK][FB]  비밀번호 확인 (JS 공백체크 + 서버 DB 비교)
[OK][F]   파일 개수/크기/총용량 체크 (기존 파일 합산)

[TODO][B]  제목/내용 빈값 서버 검증 없음
[TODO][B]  파일 개수/크기 서버 검증 없음 (작성과 동일)
[TODO][B]  파일 확장자 검증 없음

===========================================================================
5. 게시글 삭제 (detail.jsp / BoardController.delete)
===========================================================================

[OK][FB]  비밀번호 확인 (JS 공백체크 + 서버 DB 비교)
[OK][F]   삭제 전 confirm 창 표시

[TODO][B]  본인 글(로그인 유저)만 삭제 가능하도록 제한 없음
           → 현재는 비밀번호만 알면 누구든 삭제 가능
           → session의 userNo와 board의 user_no 비교 로직 추가 고려

===========================================================================
6. 답변 등록 (reply.jsp / BoardController.replyProc)
===========================================================================

[OK][F]   제목/내용/비밀번호 빈값 체크 (validate 함수)
[OK][F]   파일 개수/크기/총용량 체크
[OK][F]   계층 깊이 2레벨 초과 시 버튼 숨김 (detail.jsp reLev >= 2)
[OK][F]   부모글 직접 자식 5개 초과 시 버튼 숨김 (detail.jsp replyCount >= 5)
[OK][B]   replyView에서 5개 초과 시 서버 이중 차단 (countChildReply)
[OK][B]   replyProc에서 5개 초과 시 서버 이중 차단 (countChildReply)

[TODO][B]  계층 깊이 제한(reLev >= 2) 서버 측 검증 없음
           → JS 우회 시 3레벨 이상 답글 등록 가능
           → replyProc에 reLev >= 2 체크 추가 필요
[TODO][B]  제목/내용/비밀번호 빈값 서버 검증 없음
[TODO][B]  파일 개수/크기 서버 검증 없음

===========================================================================
7. 파일 다운로드 (BoardController.download)
===========================================================================

[TODO][B]  로그인 여부와 무관하게 fileNo만 알면 다운로드 가능
           → 인터셉터가 적용돼 있으면 괜찮지만 확인 필요
[TODO][B]  Path Traversal 방어 없음
           → filePath가 DB에 저장된 UUID_파일명 형식이므로 현재는 안전하지만
              명시적 방어 코드(정규표현식 또는 Paths.get 사용) 추가 권장

===========================================================================
8. 검색 (list.jsp / BoardMapper.selectBoardList)
===========================================================================

[OK][B]   MyBatis #{} 파라미터 바인딩 사용 → SQL Injection 방어됨

[TODO][F]  searchKeyword maxlength="100" 추가됨 (완료)
[TODO][B]  XSS 방어: ${board.title} 등 EL 출력 시 HTML 이스케이프 필요
           → c:out 태그 또는 fn:escapeXml 사용 권장
           → 현재 ${board.title}, ${board.content} 등이 raw 출력 중

===========================================================================
9. 공통 보안
===========================================================================

[OK][B]   로그인 인터셉터(LoginInterceptor): 세션 없으면 login.do로 리다이렉트

[TODO][B]  CSRF 방어 없음
           → POST 요청이지만 CSRF 토큰 미적용
           → 전자정부 프레임워크 CSRF 방어 기능 또는 Spring Security 적용 고려

[TODO][B]  세션 고정 공격(Session Fixation) 방어 없음
           → 로그인 성공 시 session.invalidate() 후 새 세션 생성 권장

[TODO][B]  비밀번호 암호화 미적용 (회원 비밀번호, 게시글 비밀번호 모두 평문)
           → 회원 비밀번호: BCrypt 단방향 해시 필수
           → 게시글 비밀번호: 단방향 해시 또는 최소 AES 암호화 고려

===========================================================================
우선순위 정리 (보안 관점)
===========================================================================

[긴급] 비밀번호 평문 저장 → BCrypt 암호화 적용
[높음] 파일 확장자 검증 → 악성 파일 업로드 차단
[높음] 파일 크기 서버 측 제한 → Spring multipart maxFileSize 설정
[높음] 답글 계층 깊이 서버 검증 추가 (reLev >= 2)
[중간] 제목/내용 빈값 서버 검증 추가
[중간] XSS 방어 (c:out 사용)
[중간] 로그인 시도 횟수 제한
[낮음] CSRF 토큰 적용
[낮음] 세션 고정 공격 방어

===========================================================================
