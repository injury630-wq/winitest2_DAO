<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!--
    namespace = "boardDAO" → BoardDAO.java에서 "boardDAO.{queryId}"로 호출
    모든 파라미터/결과는 Map 기반 (VO 미사용)
-->
<mapper namespace="boardDAO">

    <!-- 공통 검색 조건 SQL -->
    <sql id="searchCondition">
        <where>
	        <if test="searchType != null and searchType != '' and searchKeyword != null and searchKeyword != ''">
	            <choose>
	                <when test="searchType == 'title'">
	                    AND b.title LIKE CONCAT('%', #{searchKeyword}, '%')
	                </when>
	
	                <when test="searchType == 'writer_name'">
	                    AND u.user_name LIKE CONCAT('%', #{searchKeyword}, '%')
	                </when>
	            </choose>
	        </if>
	
	        <if test="(searchType == null or searchType == '') and searchKeyword != null and searchKeyword != ''">
	            AND (
	                title LIKE CONCAT('%', #{searchKeyword}, '%')
	                OR u.user_name LIKE CONCAT('%', #{searchKeyword}, '%')
	            )
	        </if>
	    </where>
    </sql>

    <!-- ===== 게시글 조회 ===== -->

    <!-- 게시글 목록 조회 (페이징 + 검색) -->
    <select id="selectBoardList" parameterType="map" resultType="map">
        SELECT b.board_no                           AS boardNo
        	 , b.title                              AS title
         	 , u.user_name                          AS writerName
         	 , b.hit                                AS hit
        	 , DATE_FORMAT(b.reg_date, '%Y-%m-%d')  AS regDate
        	 , b.ref                                AS ref
       		 , b.re_lev                             AS reLev
        	 , b.re_seq                             AS reSeq
             /* 해당 게시글에 첨부파일이 있는지 여부 (0 = 없음, 1 이상 = 있음) */
             , (SELECT COUNT(*) FROM board_file bf WHERE bf.board_no = b.board_no) AS fileCount
    	FROM board b
    	JOIN users u ON b.user_no = u.user_no
	    <where>
	        <if test="searchType != null and searchType != '' and searchKeyword != null and searchKeyword != ''">
	            <choose>
	                <when test="searchType == 'title'">
	                    AND b.title LIKE CONCAT('%', #{searchKeyword}, '%')
	                </when>
	
	                <when test="searchType == 'writer_name'">
	                    AND u.user_name LIKE CONCAT('%', #{searchKeyword}, '%')
	                </when>
	            </choose>
	        </if>
	
	        <if test="(searchType == null or searchType == '') and searchKeyword != null and searchKeyword != ''">
	            AND (
	                title LIKE CONCAT('%', #{searchKeyword}, '%')
	                OR u.user_name LIKE CONCAT('%', #{searchKeyword}, '%')
	            )
	        </if>
	    </where>
        ORDER BY ref DESC, re_seq ASC
        LIMIT #{firstIndex}, #{recordCountPerPage}
    </select>

    <!-- 게시글 전체 건수 (페이징용) -->
    <select id="selectBoardTotalCount" parameterType="map" resultType="int">
        SELECT COUNT(*)
        FROM board b 
        JOIN users u ON b.user_no = u.user_no
        <include refid="searchCondition"/>
    </select>

    <!-- 게시글 단건 상세 조회 -->
    <select id="getBoardDetail" parameterType="map" resultType="map">
SELECT b.board_no                              AS boardNo
             , b.title                                AS title
             , b.content                              AS content
             , u.user_name                            AS writerName
             , b.writer_pw                            AS writerPw
             , b.hit                                  AS hit
             , b.ref                                  AS ref
             , b.re_lev                               AS reLev
             , b.re_seq                               AS reSeq
             , b.parent_no                            AS parentNo
             , DATE_FORMAT(b.reg_date, '%Y-%m-%d')    AS regDate
             , b.modi_date							  AS modiDate <!-- 현재 사용 x -->
             , b.user_no								AS userNo
        FROM board b
    	JOIN users u ON b.user_no = u.user_no
        WHERE b.board_no = #{boardNo}
</select>

    <!-- 게시글 처리  -->

    <!-- 조회수 증가 -->
    <update id="updateHit" parameterType="map">
        UPDATE board
        SET hit = hit + 1
        WHERE board_no = #{boardNo}
    </update>

    <!-- 게시글 등록 (useGeneratedKeys로 boardNo 자동 반환) -->
    <!-- parent_no: 원글은 null, 답변글은 부모글의 board_no -->
    <insert id="insertBoard" parameterType="map" useGeneratedKeys="true" keyProperty="boardNo">
        INSERT INTO board (title, content, user_no, writer_pw, ref, re_lev, re_seq, hit, parent_no, reg_date)
        VALUES (#{title}, #{content}, #{userNo}, #{writerPw}, #{ref}, #{reLev}, #{reSeq}, 0, #{parentNo}, NOW())
    </insert>

    <!-- ref 업데이트: 원글 등록 후 boardNo로 자기 참조 -->
    <update id="updateRef" parameterType="map">
        UPDATE board
        SET ref = #{boardNo}
        WHERE board_no = #{boardNo}
    </update>

    <!-- 게시글 수정 -->
    <update id="updateBoard" parameterType="map">
        UPDATE board
        SET title      = #{title}
          , content    = #{content}
          , modi_date  = NOW()
        WHERE board_no = #{boardNo}
    </update>

    <!-- 직접 자식글 조회 (parent_no 기반 계층 삭제용) -->
    <select id="selectChildBoardNos" parameterType="map" resultType="map">
        SELECT board_no AS boardNo
        FROM board
        WHERE parent_no = #{boardNo}
    </select>

    <!-- 게시글 복수 삭제 (계층 삭제용) -->
    <delete id="deleteBoardByIds" parameterType="map">
        DELETE FROM board
        WHERE board_no IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <!-- 특정 글(boardNo)의 직접 자식 수 조회 (부모글당 최대 5개 답글 제한 체크용)
         parent_no = #{boardNo} : 이 글을 부모로 둔 직접 자식만 카운트
         원글(a1)에 5개, 그 자식(a1-1)에 또 5개 등 각 부모별 독립적으로 제한 -->
    <select id="countChildReply" parameterType="map" resultType="int">
        SELECT COUNT(*)
        FROM board
        WHERE parent_no = #{boardNo}
    </select>

    <!-- 답변글 re_seq 밀기 (같은 ref 내 내 위치 이후 항목 +1) -->
    <update id="updateReSeq" parameterType="map">
        UPDATE board
        SET re_seq = re_seq + 1
        WHERE ref = #{ref}
          AND re_seq > #{reSeq}
    </update>

    <!-- ===== 첨부파일 ===== -->

    <!-- 첨부파일 등록 -->
    <insert id="insertBoardFile" parameterType="map">
        INSERT INTO board_file (board_no, file_name, file_path, file_size)
        VALUES (#{boardNo}, #{fileName}, #{filePath}, #{fileSize})
    </insert>

    <!-- 첨부파일 목록 조회 -->
    <select id="getBoardFileList" parameterType="map" resultType="map">
        SELECT file_no   AS fileNo
             , board_no  AS boardNo
             , file_name AS fileName
             , file_path AS filePath
             , file_size AS fileSize
        FROM board_file
        WHERE board_no = #{boardNo}
    </select>

    <!-- 첨부파일 단건 조회 -->
    <select id="getBoardFileDetail" parameterType="map" resultType="map">
        SELECT file_no   AS fileNo
             , board_no  AS boardNo
             , file_name AS fileName
             , file_path AS filePath
             , file_size AS fileSize
        FROM board_file
        WHERE file_no = #{fileNo}
    </select>

    <!-- 첨부파일 단건 삭제 -->
    <delete id="deleteBoardFile" parameterType="map">
        DELETE FROM board_file
        WHERE file_no = #{fileNo}
    </delete>

    <!-- 첨부파일 목록 조회 (게시글 복수 ID 기반 - 계층 삭제 전 물리 파일 경로 수집용) -->
    <select id="selectFileListByIds" parameterType="map" resultType="map">
        SELECT file_no   AS fileNo
             , board_no  AS boardNo
             , file_name AS fileName
             , file_path AS filePath
             , file_size AS fileSize
        FROM board_file
        WHERE board_no IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <!-- 첨부파일 복수 삭제 (계층 삭제용) -->
    <delete id="deleteBoardFilesByBoardIds" parameterType="map">
        DELETE FROM board_file
        WHERE board_no IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

</mapper>
