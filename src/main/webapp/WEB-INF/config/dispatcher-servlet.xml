<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
		xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xmlns:p="http://www.springframework.org/schema/p"
        xmlns:context="http://www.springframework.org/schema/context"
        xmlns:mvc="http://www.springframework.org/schema/mvc"
        xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.0.xsd
                http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.0.xsd
                http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc-4.0.xsd">

	<!-- MVC 어노테이션 활성화 -->
	<mvc:annotation-driven />

	<!-- 정적 리소스 서빙: /resources/** → /webapp/resources/ 당장 필요하지 않은 파일 설정 x-->
	<!-- <mvc:resources mapping="/resources/**" location="/resources/"/> -->

	<!-- 컨트롤러 컴포넌트 스캔 -->
	<context:component-scan base-package="wini.winitest" use-default-filters="false">
        <context:include-filter type="annotation" expression="org.springframework.stereotype.Controller"/>
        <context:exclude-filter type="annotation" expression="org.springframework.stereotype.Service"/>
        <context:exclude-filter type="annotation" expression="org.springframework.stereotype.Repository"/>
    </context:component-scan>
    <context:component-scan base-package="egovframework.com" use-default-filters="false">
    	<context:include-filter type="annotation" expression="org.springframework.stereotype.Controller"/>
    	<context:exclude-filter type="annotation" expression="org.springframework.stereotype.Service"/>
        <context:exclude-filter type="annotation" expression="org.springframework.stereotype.Repository"/>
	</context:component-scan>

	<!-- ===== View Resolver 체인 (order 순서대로 시도) ===== -->

	<!-- 1순위: BeanName (jsonView 등 빈 이름으로 직접 반환하는 경우) -->
	<bean class="org.springframework.web.servlet.view.BeanNameViewResolver" p:order="0"/>

	<!-- Tiles 설정: tiles-def.xml 에 정의된 뷰는 Tiles, 없으면 자동으로 JSP 리졸버로 -->
	<bean id="tilesViewResolver" class="org.springframework.web.servlet.view.UrlBasedViewResolver">
    <property name="viewClass" value="org.springframework.web.servlet.view.tiles3.TilesView" />
    <property name="order" value="1" />
</bean>

	<!-- Tiles 설정 파일 위치 -->
	<bean id="tilesConfigurer"
	      class="org.springframework.web.servlet.view.tiles3.TilesConfigurer">
	    <property name="definitions">
	        <list>
	            <value>/WEB-INF/tiles/tiles-def.xml</value>
	        </list>
	    </property>
	</bean>

	<!-- 3순위: JSP 직접 매핑 (Tiles 정의가 없는 뷰) -->
	<bean class="org.springframework.web.servlet.view.UrlBasedViewResolver" p:order="2"
		  p:viewClass="org.springframework.web.servlet.view.JstlView"
		  p:prefix="/WEB-INF/views/" p:suffix=".jsp"/>

	<!-- JSON 응답용 빈 (return "jsonView" 시 사용) 현재 @Responsebody로 사용해서 안씀 -->
	<bean id="jsonView" class="org.springframework.web.servlet.view.json.MappingJackson2JsonView">
		<property name="contentType" value="text/html;charset=UTF-8"/>
	</bean>

	<!-- 파일 업로드 설정 -->
	<bean id="multipartResolver"
	      class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
	    <property name="maxUploadSize"        value="52428800"/>  <!-- 50MB: 요청 전체 합산 제한 -->
	    <property name="maxInMemorySize"      value="1048576"/>   <!--  1MB: 디스크 저장 전 메모리 임시 버퍼 -->
	    <property name="maxUploadSizePerFile" value="10485760"/>  <!-- 10MB: 파일 1개당 제한 -->
	</bean>


<!-- ===== 인터셉터 체인 (선언 순서대로 실행) ===== -->
<mvc:interceptors>
   	<!-- 1. 로그인 인터셉터 - 세션 없으면 로그인 페이지로 이동 -->
   <mvc:interceptor>
       <mvc:mapping path="/**/*.do"/>
       <mvc:exclude-mapping path="/user/login.do"/>
       <mvc:exclude-mapping path="/user/loginProc.do"/>
       <mvc:exclude-mapping path="/user/register.do"/>
       <mvc:exclude-mapping path="/user/registerProc.do"/>
       <mvc:exclude-mapping path="/user/idCheck.do"/>
       <bean class="wini.winitest.interceptor.LoginInterceptor"/>
   </mvc:interceptor>

   <!-- 2. 권한 인터셉터 - 등록만 ADMIN 이상 (수정/삭제는 본인도 가능하므로 서비스에서 판단) -->
   <mvc:interceptor>
       <mvc:mapping path="/user/userRegist.do"/>
       <mvc:mapping path="/user/ajaxTest.do"/>
       <bean class="wini.winitest.interceptor.RoleInterceptor">
           <constructor-arg name="minRoleRank" value="70020" type="int"/>
       </bean>
   </mvc:interceptor>

   <!-- 3. 갤러리 수정 폼 접근 제어 - 작성자 또는 관리자만 허용 (수정/삭제 저장은 컨트롤러에서 체크) -->
   <mvc:interceptor>
       <mvc:mapping path="/gallery/form.do"/>
       <bean class="wini.winitest.interceptor.GalleryEditInterceptor">
           <constructor-arg ref="galleryDAO"/>
       </bean>
   </mvc:interceptor>

</mvc:interceptors>


</beans>
